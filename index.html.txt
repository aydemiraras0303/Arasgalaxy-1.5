<!DOCTYPE html>
<html lang="tr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Aras Galaxy Online v1.0</title>
<style>
  body {
    margin: 0;
    overflow: hidden;
    background: linear-gradient(120deg, #0a0f25, #1a1f4b);
    color: white;
    font-family: 'Arial';
  }
  #ui {
    position: absolute;
    top: 10px;
    left: 10px;
    z-index: 10;
  }
  #money, #mission, #chat {
    margin: 10px;
    padding: 10px;
    background: rgba(0,0,0,0.5);
    border-radius: 10px;
  }
  #marketBtn {
    position: absolute;
    bottom: 20px;
    right: 20px;
    background: #ff9d00;
    color: black;
    padding: 10px 20px;
    border-radius: 20px;
    cursor: pointer;
    font-weight: bold;
  }
  #marketBtn:hover { background: #ffc14d; }
</style>
</head>
<body>

<div id="ui">
  <div id="money">💰 Para: 0</div>
  <div id="mission">🎯 Görev: Başla</div>
</div>
<div id="marketBtn">🛍️ Market</div>

<script src="https://cdn.jsdelivr.net/npm/three@0.158.0/build/three.min.js"></script>
<script>
// === SAHNE OLUŞTURMA ===
const scene = new THREE.Scene();
const camera = new THREE.PerspectiveCamera(75, innerWidth/innerHeight, 0.1, 1000);
const renderer = new THREE.WebGLRenderer();
renderer.setSize(innerWidth, innerHeight);
document.body.appendChild(renderer.domElement);

// === IŞIKLAR ===
const light = new THREE.PointLight(0xffffff, 2);
light.position.set(0, 10, 10);
scene.add(light);

// === OYUNCU ===
const playerGeometry = new THREE.BoxGeometry(1, 2, 1);
const playerMaterial = new THREE.MeshStandardMaterial({color: 0x00ffcc});
const player = new THREE.Mesh(playerGeometry, playerMaterial);
scene.add(player);
player.position.y = 1;

// === ZEMİN ===
const ground = new THREE.Mesh(
  new THREE.PlaneGeometry(100, 100),
  new THREE.MeshStandardMaterial({color: 0x202020})
);
ground.rotation.x = -Math.PI/2;
scene.add(ground);

// === KAMERA ===
camera.position.set(0, 3, 5);
camera.lookAt(player.position);

// === HAREKET ===
let keys = {};
window.addEventListener('keydown', e => keys[e.key] = true);
window.addEventListener('keyup', e => keys[e.key] = false);

let money = 0;
let mission = 1;
let currentZone = "city";

function movePlayer() {
  const speed = 0.2;
  if (keys['w']) player.position.z -= speed;
  if (keys['s']) player.position.z += speed;
  if (keys['a']) player.position.x -= speed;
  if (keys['d']) player.position.x += speed;
  camera.position.x = player.position.x;
  camera.position.z = player.position.z + 5;
}

// === GÖREVLER ===
const missions = [
  "Uzay üssüne git 🚀",
  "Şehirde marketi bul 🏙️",
  "Renkli dünyada dans et 🎨",
  "Evine mobilya yerleştir 🪑",
  "10 para topla 💰",
  "Yeni skin al 👕",
  "Bir arkadaşla konuş 💬",
  "Görev panelini aç 🎯",
  "Market ikonuna bas 🛍️",
  "Görevleri tamamla 🌟"
];

// === MARKET ===
const marketBtn = document.getElementById("marketBtn");
marketBtn.onclick = () => {
  alert("🎉 Market bölgesine ışınlandın!");
  player.position.set(0,1,-20);
};

// === GÖREV SİSTEMİ ===
function updateMission() {
  document.getElementById("mission").innerText = "🎯 Görev: " + missions[mission-1];
  document.getElementById("money").innerText = "💰 Para: " + money;
}

// === ANA DÖNGÜ ===
function animate() {
  requestAnimationFrame(animate);
  movePlayer();
  renderer.render(scene, camera);
  updateMission();
}
animate();

// === PARA TOPLAMA (Sanal) ===
setInterval(() => {
  money += 1;
  if (money % 5 === 0 && mission < missions.length) mission++;
}, 2000);
</script>

</body>
</html>